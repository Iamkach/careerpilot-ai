# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Claude / tooling
#.claude/

# Secrets & credentials
config/gmail_credentials.json
config/token.json
*.env
.env

# Stage 7 application answers (personal details — written by `run.py --setup-profile`)
config/application_profile.json

# Personal identity/profile overlay (written by `run.py --init`; placeholders in config/profile.example.json)
config/profile.json

# Personal resume template (generate via scripts/make_resume_template.py or drop your own)
config/resume_template.docx

# Discovered ATS tokens cache (per-user, regenerated by scripts/sources.py)
config/ats_tokens.json

# Stage 7 Layer 3 browser-extension bridge token (regenerated per `run.py --serve` invocation)
config/extension_token.txt

# Generated output
output/

# Brace-expansion artifact dir
'{scripts,config,output}'/

# OS
.DS_Store
Thumbs.db
docs/architecture/ai-analysis/