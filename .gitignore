# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Claude / tooling
#.claude/

# Secrets & credentials
config/gmail_credentials.json
config/token.json
*.env
.env

# Stage 7 application answers (personal details — written by `run.py --setup-profile`)
config/application_profile.json

# Step 11 fork profile (personal details — see docs/backlog/step-11-forkable-setup.md)
config/profile.json

# Personal resume template (generate via scripts/make_resume_template.py or drop your own)
config/resume_template.docx

# Generated output
output/

# Brace-expansion artifact dir
'{scripts,config,output}'/

# OS
.DS_Store
Thumbs.db
docs/architecture/ai-analysis/