# End goal

Plain language. What is true once this ships that isn't true today? Describe the target state,
not the implementation — this is the section an agent re-reads mid-implementation to check it
hasn't drifted off target. If you can't state it without describing *how* you'll build it, you
haven't found the end goal yet.
