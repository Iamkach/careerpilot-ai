# Constraints

Hard limits any valid implementation must respect — binding decisions, governing rules, security
invariants, performance/compatibility bounds. Not preferences; things that make an implementation
wrong if violated.
