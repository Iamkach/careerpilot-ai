# Core — AI job search pipeline
claude-agent-sdk>=0.2.0    # Claude Code subscription via Agent SDK (default: AI_PROVIDER=claude_code + workflow.py)
anthropic>=0.40.0          # Claude metered API (only used by AI_PROVIDER="claude")
requests>=2.31.0           # HTTP client for Apify
notion-client>=2.2.1,<2.6  # Primary data store (job tracker DB); 2.6+/3.x dropped databases.query (data_sources API)

# Optional providers (only needed if AI_PROVIDER is changed)
google-generativeai>=0.8.0 # Gemini provider
openai>=1.40.0             # Codex/OpenAI provider
docxtpl>=0.18.0            # Render tailored resume into a .docx template (stage 2)

# Optional — Stage 4 Gmail digest sending
google-auth>=2.30.0
google-api-python-client>=2.130.0
